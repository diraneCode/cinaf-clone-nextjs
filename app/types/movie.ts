export type movie = {
    id?: number,
    title: string,
    image: string
    timeoutRef: React.MutableRefObject<number>;
}
export type movie2 = {
    id?: number,
    title: string,
    image: string
}